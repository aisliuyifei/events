<%= form_for @setting, :html => { :class => "form-horizontal setting" } do |f| %>

  <% if @setting.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@setting.errors.count, "error") %> prohibited this setting from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @setting.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :head, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :head, :class => 'form-control' %>
    </div>
    <%=f.error_span(:head) %>
  </div>
  <div class="form-group">
    <%= f.label :foot, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :foot, :class => 'form-control' %>
    </div>
    <%=f.error_span(:foot) %>
  </div>
  <div class="form-group">
    <%= f.label :title, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :title, :class => 'form-control' %>
    </div>
    <%=f.error_span(:title) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                settings_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
